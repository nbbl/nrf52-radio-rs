{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Beacon",
            "type": "shell",
            "command": "export DEFMT_LOG=info && cargo build --bin ble_beacon"
        },
        {
            "label": "Build Blinky Embassy",
            "type": "shell",
            "command": "export DEFMT_LOG=info && cargo build --bin blinky_embassy"
        },
        {
            "label": "Build SSD1306 Screen",
            "type": "shell",
            "command": "export DEFMT_LOG=info && cargo build --bin ssd1306_screen"
        },
        {
            "label": "Build Sensor Reading",
            "type": "shell",
            "command": "export DEFMT_LOG=info && cargo build --bin sensor_reading"
        }
    ]
}